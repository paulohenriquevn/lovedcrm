'use client'

export function MainNav(): JSX.Element {
  return (
    <div className="flex items-center">
      {/* MainNav is now empty - navigation is handled directly in navbar.tsx */}
    </div>
  )
}
